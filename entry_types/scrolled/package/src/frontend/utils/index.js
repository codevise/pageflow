import {camelize} from './camelize';
import {
  isBlank,
  isBlankEditableTextValue,
  presence,
} from './blank';

export const utils = {
  camelize,
  isBlank,
  isBlankEditableTextValue,
  presence
}
