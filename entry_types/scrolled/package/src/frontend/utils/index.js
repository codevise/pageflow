import {capitalize} from './capitalize';
import {camelize} from './camelize';
import {
  isBlank,
  isBlankEditableTextValue,
  presence,
} from './blank';

export const utils = {
  capitalize,
  camelize,
  isBlank,
  isBlankEditableTextValue,
  presence
}
