toc:
  - name: Components
    description: |
      React components for building content elements.
    children:
      - Image
  - name: Hooks
    description: |
      React hooks to obtain entry data.
    children:
      - useFile
