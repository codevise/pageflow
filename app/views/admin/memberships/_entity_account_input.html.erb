<%= f.input :entity,
            label: I18n.t('activerecord.attributes.pageflow/membership.account'),
            collection: membership_accounts_collection(parent, resource),
            include_blank: false,
            input_html: { disabled: !f.object.new_record? || parent_type == :account } %>
